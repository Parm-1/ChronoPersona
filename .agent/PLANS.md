# Active Plans

Exactly one plan is active for the current write-active deliverable:

- `.agent/plans/active-pythia-local-feasibility.md` — acquire the immutable
  final Pythia artifact, measure local loading/logits feasibility, then decide
  whether to implement and run a tiny training/resume benchmark.
