# Active Plans

Exactly one plan is active for the current write-active deliverable:

- `.agent/plans/active-verified-registry-loader.md` — publish the accepted
  exact-head Pythia tokenizer boundary evidence and require final PR CI; E5
  passed, E6 remains active, and model scoring stays blocked behind its
  successor resource/load gate.

Completed plans remain under `.agent/plans/` as durable history. In particular,
`active-pythia-local-feasibility.md` records the completed v0/v1 loading and
tiny-LoRA feasibility work and is no longer write-active.
